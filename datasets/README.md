# Environmental datasets

The environmental datasets used in this study originate primarily
from the PFAS Data Hub and from additional public monitoring sources
in the United States.

The exact dataset snapshot used for the manuscript is archived
separately on Zenodo. Raw files are not stored in the Git repository.

Place the downloaded source files in `datasets/raw/` using the
filenames documented in the dataset inventory.
